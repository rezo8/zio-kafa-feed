server {
  serverMetadata {
    port = 8080
    port = ${?SERVER_PORT}
  }
  reader{
    consumerCount = 4,
    parallelReads = 1
  }
  consumer {
    topicName = "people",
    bootstrapServers = ["localhost:29092"],
    maxPollRecords = 10
  }
}

ingestionJob {
    batchSize = 500
    producerConfig {
     topicName = "people",
     bootstrapServers = ["localhost:29092"]
    }
}
